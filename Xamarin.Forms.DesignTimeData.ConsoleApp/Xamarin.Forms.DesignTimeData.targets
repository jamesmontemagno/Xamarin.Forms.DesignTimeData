
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

	<UsingTask TaskName="Xamarin.Forms.DesignTimeData.BuildTasks.DesignTimeDataTask" AssemblyFile="$(MSBuildThisFileDirectory)Xamarin.Forms.DesignTimeData.BuildTasks.BuildTasks.dll" />

	<!-- DesignTimeDataTask -->
	<PropertyGroup>
		<CompileDependsOn>
      $(CompileDependsOn);
      DesignTimeDataTask;
    </CompileDependsOn>
  </PropertyGroup>

  <Target Name="DesignTimeDataTask" AfterTargets="CopyFilesToOutputDirectory" DependsOnTargets="CopyFilesToOutputDirectory" Condition="'$(_DesignTimeDataTaskAlreadyExecuted)'!='true'">
		<PropertyGroup>
      <_DesignTimeDataTaskAlreadyExecuted>true</_DesignTimeDataTaskAlreadyExecuted>
		</PropertyGroup>
    <DesignTimeDataTask
			Assembly = "$(OutputPath)$(TargetFileName)"
			OutputPath = "$(OutputPath)"
			ReferencePath = "@(ReferencePath)" 
            Configuration = "$(Configuration)"/>
	</Target>

</Project>